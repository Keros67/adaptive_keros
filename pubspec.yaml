name: adaptive_keros
description: Apprendre à créer un package qui s'adapte selon l'OS utilisé.
version: 0.0.2
homepage: https://github.com/Keros67/adaptive_keros

environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter: